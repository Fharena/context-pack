interface:
  display_name: "Context Pack"
  short_description: "Build task-specific repo context packs"
  default_prompt: "Use $context-pack to prepare focused repo context before reviewing, debugging, or continuing work."
policy:
  allow_implicit_invocation: true
