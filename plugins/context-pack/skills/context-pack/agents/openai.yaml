interface:
  display_name: "Context Pack"
  short_description: "Build task-specific repo context packs"
  default_prompt: "Use $context-pack to initialize project memory or build a focused context pack before reviewing code."
policy:
  allow_implicit_invocation: true
