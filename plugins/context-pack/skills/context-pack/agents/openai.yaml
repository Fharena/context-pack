interface:
  display_name: "Context Pack"
  short_description: "Route agents to bounded repo evidence"
  default_prompt: "Use $context-pack to load the smallest useful source evidence, then continue the task."
policy:
  allow_implicit_invocation: true
