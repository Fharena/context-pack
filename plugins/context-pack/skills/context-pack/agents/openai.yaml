interface:
  display_name: "Context Pack"
  short_description: "Autonomously build focused repo context packs"
  default_prompt: "Use $context-pack proactively before broad repo reading, review, debugging, or handoff."
policy:
  allow_implicit_invocation: true
